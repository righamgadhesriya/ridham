# Quant Strategy Backtester

A Streamlit web app that runs a long-only SMA crossover backtest using `yfinance` data.

## Run

```bash
pip install -r requirements.txt
streamlit run app.py
```

Then click **Run Backtest**.
